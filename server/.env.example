# Server configuration
PORT=5175

# Remote APEX API
APEX_BASE=https://apex.realcore.group:8443/ords/realcore/controlling
APEX_USERNAME=realcore_ctrl_user
APEX_PASSWORD=REPLACE_ME

# Default query parameters (can be overridden via request query)
DEFAULT_UNIT=h0zDeGnQIgfY3px
DEFAULT_DATUM_VON=2024-10-01T00:00:00Z
DEFAULT_DATUM_BIS=2025-05-30T00:00:00Z

# --- SMTP Mail (e.g. Strato) ---
# Host and port. Use 465 with secure=true (SSL/TLS) or 587 with secure=false (STARTTLS)
SMTP_HOST=smtp.strato.de
SMTP_PORT=465
SMTP_SECURE=true
# Auth user and password
SMTP_USER=m.banner@futurestore.shop
SMTP_PASS=REPLACE_ME
# Optional FROM (defaults to SMTP_USER if omitted)
SMTP_FROM=m.banner@futurestore.shop
# Default recipient for /api/mail/test or when 'to' is omitted
SMTP_DEFAULT_RECIPIENT=

# --- PDF/Reports ---
# Optional Logo for PDF header (public URL to PNG/JPG)
PDF_LOGO_URL=https://realcore.info/bilder/rc-logo.png
